{
  "name": "deserve-trade-api",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8787",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy --config wrangler.jsonc",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@noble/ed25519": "^2.2.3",
    "@supabase/supabase-js": "^2.93.1",
    "bs58": "^6.0.0",
    "hono": "^4.11.9",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.3",
    "vite": "^6.3.5",
    "vite-ssr-components": "^0.5.2",
    "wrangler": "^4.17.0"
  }
}
